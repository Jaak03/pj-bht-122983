#!/bin/sh

alias artisan="docker compose exec app php /app/artisan"
alias composer="docker compose exec app composer"
alias pest="docker compose exec app /app/vendor/bin/pest"
alias npm="docker compose exec app npm"